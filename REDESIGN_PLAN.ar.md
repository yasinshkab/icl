# خطة إعادة تصميم موقع ICL

قراءة التصميم: إعادة تصميم صفحة هبوط B2B للشركاء/العملاء، طابع «عصري مؤسسي»، مبني على Tailwind وحرَكات مقيدة.

المقاييس الأساسية
- DESIGN_VARIANCE: 6
- MOTION_INTENSITY: 5
- VISUAL_DENSITY: 4

الأهداف
- تحسين الوضوح والتسلسل البصري في صفحة الهبوط.
- الحفاظ على المحتوى والشركاء؛ تحديث المرئيات والتفاعلات.
- ضمان إمكانية الوصول، ودعم RTL، وأداء جيد.

المراحل العالية المستوى
1. التدقيق والاتجاه (مكتمل)
   - تدقيق المستودع، استخراج المحتوى، جرد الأصول
   - تأكيد اتجاه "عصري مؤسسي"

2. نظام التصميم والرموز (منفذ / جارٍ)
   - إضافة متغيرات نصف القطر، ألوان، تلطيف الخطوط
   - توحيد `--radius-*` والاختصارات

3. الطباغة والهيرو (التالي)
   - تحديث مقاييس وأنماط الخطوط في الموقع
   - ضمان الهيرو: العنوان ≤ سطرين؛ النص الفرعي ≤ 20 كلمة؛ زر الدعوة ظاهر بدون تمرير
   - تقليل استخدام الـ "eyebrow"؛ وضع شريط الشعارات تحت الهيرو

4. صقل المكونات
   - شريط التنقل: سطر واحد على سطح المكتب، ارتفاع 64–72px، قائمة هتــمبل بار قابلة للوصول
   - الأزرار: نصف قطر موحّد، فحوص التباين، تأثير ضغط `press-scale`
   - البطاقات والشبكات: استبدال صفوف الثلاث بطاقات المتكافئة بتخطيطات بنتو متنوعة
   - شريط/كاروسيل الشركاء: ماركيه واحد كحد أقصى، كبر حسب المطلوب

5. الحركة والتفاعل
   - استبدال `transition-all` (تم); تحريك `transform`/`opacity` فقط
   - دعم تقليل الحركة؛ استخدام `initial={false}` لـ AnimatePresence
   - عزل الحركات المعقدة في مكونات عميلة

6. الصور والأصول
   - صور الهيرو والصور الرئيسية (توليد أو جلب)؛ تحسين باستخدام `next/image`
   - نقل شعارات الشركاء إلى `/public/old-assets/` وتفضيل SVGs (Simple Icons)

7. الوصول، RTL، واختبار i18n
   - فحوص التباين (WCAG AA)، التنقل عبر لوحة المفاتيح، حالات النماذج
   - تحقق تخطيط اللغة العربية وخيارات الخطوط الاحتياطية

8. معاينة محلية وصقل
   - إعادة تشغيل سيرفر التطوير، فحص بصري، تكرار وفق الملاحظات

9. تحضير النشر
   - بناء، فحص Lighthouse، مراجعة الحزم

الخطوات الفورية التالية (أقوم بها عند تأكيدك)
- إكمال تحديث الطباغة في `src/app/globals.css` والمكونات الأساسية
- تعديل تركيب الهيرو في `src/components/sections/HeroSection.tsx`
- تشغيل معاينة محلية (أستطيع إيقاف سيرفر التطوير الحالي إذا سمحت)

الملفات للمراجعة بعد التغييرات
- `src/app/globals.css`
- `src/components/sections/HeroSection.tsx`
- `src/components/layout/Navbar.tsx`
- `src/components/sections/PartnerMarquee.tsx`

طلبات تصميم جديدة من المستخدم (مضافة):
- شريط التنقل: استخدم بنية شريط التنقل من الصورة الأولى لكن عدل المسافات، أنصاف القطر والألوان لتتوافق مع رموز التصميم بالمشروع. احتفظ بتخطيط سطر واحد على سطح المكتب وتأكد من التباين ومساحات النقر.
- لافتة الدعوة للعمل (CTA): استبدل الشريط الحالي بلافتة غنية (خلفية بصورة شركة، تراكب مخفف، عمود المحتوى، زر CTA بارز). استخدم `next/image` مع `priority` للافتة الهيرو وتأكد من أن زر CTA يحقق شروط التباين.
- التذييل: احذف التذييلات المكررة واستبدلها بتذييل واحد مستوحى من الصورة الثالثة (حاوية بطبقة مستديرة أعلاه وتذييل خفيف). قم بتهيئته ليتماشى مع رموز الموقع (داكن/ذهبي) وضمّن روابط الاتصال وشعارات الشركاء والروابط القانونية.

هل أتابع الآن بتطبيق تحديثات الخطوط والهيرو؟
